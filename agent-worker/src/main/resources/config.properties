portal.id=mock-portal

# Credentials are resolved by LocalFileCredentialsProvider from, in order:
#   1. $FINANCEAGENT_SECRETS_FILE
#   2. ~/.financeagent/secrets.properties
# See secrets.properties.example at the repo root.

artifacts.dir=artifacts
